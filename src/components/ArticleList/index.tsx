import { ArticleListItem } from '../ArticleListItem';

export const ArticleList = () => {

  return (
    <div className="article-list">
      <ArticleListItem
        title={''}
        url={''}
      />
    </div>
  );
};
